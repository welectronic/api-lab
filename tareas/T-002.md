---
id: T-002
titulo: Documentar GET /api/health en el README
asignado: SENIOR-2
prerevisor: ninguno
talla: S
tipo_valor: pulido
historia: US2
requisitos: [RF-003]
oleada: 1
estado: aprobada
rama: chr/T-002-readme-health
rama_origen: main
depende_de: []
irreversible: no
seguridad: normal
creada: 2026-09-24
---

# T-002 — Documentar GET /api/health en el README

## Objetivo
Que el README muestre `GET /api/health` en una tabla de endpoints, con un ejemplo de respuesta y cómo correr los tests.

## Contexto (leer solo esto)
- `SPEC.md` → US2, RF-003
- `ARQUITECTURA.md` → C-001 y C-002
- **Patrón de referencia a seguir:** las tablas `Método | Ruta | Descripción` de la sección *Endpoints* de `README.md`

## Archivos permitidos
- Modificar: `README.md`
- **No tocar:** cualquier otro archivo

## Contrato a respetar
```
GET /api/health
→ 200 OK
  {"status":"ok"}
```
Y el comando de tests: `npm test` (sin MongoDB ni SQLite).

## Criterios de aceptación
| # | Criterio | Cómo se verifica | Falla cuando |
|---|---|---|---|
| 1 | Nueva subsección en *Endpoints* (ej. `### Salud`) con una tabla del mismo formato que las existentes y la fila `GET` \| `/api/health` | leer el diff de `README.md` | falta la fila, la ruta no es exactamente `/api/health` o la tabla no sigue el formato de las demás |
| 2 | Ejemplo de respuesta en bloque ```json idéntico al contrato | leer el diff | el ejemplo tiene campos distintos a `{"status":"ok"}` o no indica `200` |
| 3 | En *Uso*, una línea `npm test` indicando que no requiere bases de datos | leer el diff | falta o dice que necesita `.env`/MongoDB |
| 4 | Solo `README.md` cambia y el resto del README queda igual | `git diff --stat origin/main...HEAD` | aparece otro archivo, o se reescriben secciones existentes |

## Fuera de alcance
- Implementar el endpoint o el script `test` (es T-001, en paralelo; tu rama no los tendrá y está bien).
- Reescribir o traducir el resto del README.

## Notas del LIDER
- Verificación para esta tarea: `git diff --stat origin/main...HEAD` y `git diff origin/main...HEAD` pegados en el reporte. `npm test` aún no existe en tu rama (lo agrega T-001): no lo corras ni lo reportes como falla.
- Nombre de rama **exacto** `chr/T-002-readme-health`. No abras PR.
- **GitHub NO te va a impedir empujar a `main`** (usas la cuenta admin del PO, D-5). Push solo de `chr/T-002-readme-health` y de `charter`. Un push a `main` es rechazo automático de la tarea.
