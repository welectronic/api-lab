---
tarea: T-003
ronda: 1
revisor: LIDER
commit_revisado: e93c7b7
veredicto: CAMBIOS_SOLICITADOS
fecha: 2026-09-24
inicio: 2026-09-24 16:35
fin: 2026-09-24 16:46
sesiones: 1
---

# Revisión T-003 — ronda 1

## Veredicto: CAMBIOS SOLICITADOS (retenida hasta la decisión del PO)

El trabajo cumple C-003 tal como lo escribí. El hueco está en el contrato, no en la ejecución: C-003 solo cubría dos tipos de error de body y los demás siguen filtrando el stack. **No activar la ronda 2 hasta que el PO elija la opción A o B (abajo).** Con B, esta revisión pasa a APROBADA sin ronda 2.

## Verificación
- Tests (LIDER, worktree aislado, Node v24.13.1, sin `.env`): integración simulada `main` @ `99531f4` + T-003 + T-004 + T-005 → `npm test` → `tests 12, pass 12, fail 0`, exit 0.
- Criterios 1–8 y A1–A6: cumplidos. El reporte muestra que los tests fallan 9 de 9 contra `main` sin cambios, así que detectan el comportamiento anterior.
- Archivos fuera de lo permitido: ninguno (`app.js`, `tests/http-hardening.test.js`). `package*.json` sin cambios.
- Orden en `app.js` correcto: disable → headers → parsers con `limit: '100kb'` → routers intactos → 404 → manejador de 4 parámetros con `err.type` y `next(err)`.
- Pruebas propias fuera del contrato: `DELETE /api/nada` → 404 JSON; urlencoded malformado → se parsea y termina en 404 JSON (sin filtración).
- `npm audit`: 22 = línea base. `origin/main` sigue en `99531f4` (D-5).
- Pre-revisión: no se hizo (RAPIDO no se activó antes de la revisión).

## Seguridad
- [x] Casos de abuso con test, y fallan como se espera (A1–A6)
- [x] Autorización en servidor, por objeto: no aplica
- [ ] Entrada validada y salida escapada: **hallazgo 1**
- [x] Sin secretos, datos reales ni logs con datos sensibles
- [ ] Escaneos: gitleaks y Semgrep no están instalados (E3). `git grep` de credenciales sin hallazgos
- [x] Dependencias nuevas: ninguna
- [x] Ningún control de seguridad desactivado
- [ ] Coherente con el modelo de amenazas: la fila "Respuestas de error y headers" promete que no se filtra el stack; el hallazgo 1 la contradice

## Hallazgos tras triage
| # | Ubicación | Hallazgo | Severidad | Destino |
|---|---|---|---|---|
| 1 | `app.js` (manejador de errores de body) | Los otros errores de body-parser pasan con `next(err)` al manejador por defecto de Express, que responde **HTML con stack trace y rutas absolutas** cuando `NODE_ENV` no es `production` (y `npm start` no lo fija). Verificado sobre la integración: urlencoded con 1100 parámetros → `413` HTML con `PayloadTooLargeError: too many parameters … at queryparse (C:\Users\…\node_modules\body-parser\…)`; `Content-Encoding: foo` → `415` HTML con stack; `charset=klingon` → `415` HTML con stack. Con `NODE_ENV=production` el stack no sale, pero sigue siendo HTML. Es un problema **previo** (en `main` pasa con todos los errores de body), no lo introdujo la tarea | alta (vulnerabilidad verificada: filtración de información) | decision_PO |

### Decisiones para el PO (destino: decision_PO)
- **A (recomendada): corregir en ronda 2 de T-003.** Amplío C-003: todo error de body-parser (`typeof err.type === 'string'` y `err.status` entre 400 y 499) responde con su status y un cuerpo JSON fijo:
  - `400` → `{"error":"JSON malformado"}` si `err.type === 'entity.parse.failed'`; si no, `{"error":"Petición inválida"}`
  - `413` → `{"error":"Cuerpo demasiado grande"}` (incluye `parameters.too.many`)
  - `415` → `{"error":"Tipo de contenido no soportado"}`
  - cualquier otro 4xx de body → `{"error":"Petición inválida"}` con su status
  - Se agregan los casos de abuso A7 (más de 1000 parámetros), A8 (`Content-Encoding` no soportado) y A9 (charset no soportado). Costo: ~10 líneas y 3 tests. T-005 necesitaría una ronda 2 para documentar el 415.
- **B: diferir a E3.** El PO aprueba por escrito diferirla (se registra en `DECISIONES.md` como D-7). T-003 pasa a APROBADA sin cambios y el hallazgo va al backlog de E3.

## Aprendizajes
- Nuevo tropiezo: no; el error fue del LIDER al escribir el contrato. Queda en la bitácora: al endurecer un manejador de errores, enumerar **todos** los tipos que emite la librería, no solo los del requisito.
- Solución documentable: no.

## Otras notas
- La sesión corrió en Claude Code web, no local (la hora del reporte está en UTC). Mismo modelo. Propuesta del autor sobre el ROSTER → `TABLERO.md` → Propuestas al kit.
