---
tarea: T-003
ronda: 2
revisor: LIDER
commit_revisado: 266818b
veredicto: APROBADA
fecha: 2026-09-24
inicio: 2026-09-24 16:50
fin: 2026-09-24 16:54
sesiones: 1
---

# Revisión T-003 — ronda 2

## Veredicto: APROBADA

## Verificación
- Diff r1→r2 (`e93c7b7..266818b`): solo el manejador de errores de `app.js` y los tests A7–A9. Orden de `app.js` y routers sin cambios.
- Manejador según C-003 ampliado (D-7): filtra por `typeof err.type === 'string'` y status 4xx; mapea 400 + `entity.parse.failed`, 413 y 415 a los cuerpos del contrato, y el resto de 4xx a `Petición inválida`; todo lo demás va a `next(err)`. No usa `err.message` ni `err.type` en la respuesta.
- Tests (LIDER, worktree aislado, Node v24.13.1, sin `.env`): `main` @ `99531f4` + `266818b` + `0516bdc` + `d2054ca` → `npm ci && npm test` → `tests 15, pass 15, fail 0`, exit 0.
- Punta a punta sobre la integración, los tres casos del hallazgo 1 de r1: 1100 parámetros → `413 {"error":"Cuerpo demasiado grande"}`; `Content-Encoding: foo` → `415 {"error":"Tipo de contenido no soportado"}`; `charset=klingon` → `415` igual. Los tres en `application/json; charset=utf-8`, con `nosniff` y sin `X-Powered-By`. En r1 los tres respondían HTML con stack: **hallazgo 1 corregido**.
- Criterio 9: el reporte muestra A7–A9 fallando contra `e93c7b7` (responden `text/html`) y pasando en `266818b`. Coincide con lo que reproduje en r1.
- Archivos: `app.js`, `tests/http-hardening.test.js`. `package*.json` sin cambios. `npm audit` 22 = línea base. `origin/main` sigue en `99531f4` (D-5).
- Pre-revisión: no se hizo (RAPIDO no se activó).

## Seguridad
- [x] Casos de abuso A1–A9 con test, y fallan como se espera
- [x] Autorización: no aplica
- [x] Entrada validada y salida escapada: cuerpos de error fijos; no se refleja la entrada (A5, A8, A9)
- [x] Sin secretos ni datos reales
- [ ] Escaneos automáticos: gitleaks y Semgrep no están instalados (E3). `git grep` de credenciales sin hallazgos
- [x] Sin dependencias nuevas
- [x] Ningún control desactivado
- [x] Coherente con el modelo de amenazas (C-003)

## Hallazgos tras triage
| # | Ubicación | Hallazgo | Severidad | Destino |
|---|---|---|---|---|
| 1 | `app.js`, rama "otro 4xx" | Sin test: body-parser no produce otro 4xx alcanzable desde `fetch` (el autor lo explica). La rama es de 1 línea y no expone datos | baja | diferir (sin acción) |

### Diferidos al backlog
- Errores 5xx y `err.message` de los endpoints existentes: ya estaban en E3 (`PLAN.md`).

## Aprendizajes
- Nuevo tropiezo: no.
- Solución documentable: no.

## Para el PO
- Rama `chr/T-003-http-hardening` → merge a `main`. Orden sugerido: T-003, T-004, T-005. Sin conflictos entre ellas (verificado).
